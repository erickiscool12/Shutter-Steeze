console.log("this works");

var cookies = [
  "chocolate chip",
  "the lemon ones",
  "thin mints",
  "oreo",
  "samoa",
  "oatmeal"
];

function eat(food) {
  console.log(' i just ate ' + cookies);
}

var eatbutton = $(".devour")
eatbutton.on("click",
)

for (var 1 = 0; i < cookies.length; i= i + 1) {
  eat(cookies[i]); }
}
